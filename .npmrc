# Prevent hoisting for example apps
# This ensures each app gets its own Expo dependencies
node-linker=hoisted
hoist-pattern[]=*
hoist-pattern[]=!expo*
hoist-pattern[]=!@expo*

# GitHub Packages registry for @nansen-ai scope
@nansen-ai:registry=https://npm.pkg.github.com